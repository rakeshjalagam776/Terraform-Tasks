trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  ARM_SUBSCRIPTION_ID: '70857cdc-9740-4343-8a3c-e51c33db6794'
  ARM_CLIENT_ID: '<service-principal-client-id>'
  ARM_CLIENT_SECRET: '<service-principal-secret>'
  ARM_TENANT_ID: '<tenant-id>'

steps:
  - checkout: self

  - script: |
      curl -LO https://releases.hashicorp.com/terraform/1.9.0/terraform_1.9.0_linux_amd64.zip
      unzip terraform_1.9.0_linux_amd64.zip
      sudo mv terraform /usr/local/bin/
      terraform -version
    displayName: 'Install Terraform'

  - script: |
      workingDirectory: terraform   # 👈 change into the folder containing your .tf files
      terraform init -input=false
    displayName: 'Terraform Init'

  - script: |
      workingDirectory: terraform
      terraform validate
    displayName: 'Terraform Validate'

  - script: |
      workingDirectory: terraform
      terraform plan -out=tfplan -input=false
    displayName: 'Terraform Plan'

  - script: |
      workingDirectory: terraform
      terraform apply -auto-approve tfplan
    displayName: 'Terraform Apply'
